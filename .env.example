# Server-side only. Never prefix this variable with VITE_.
# Create a local .env file from this template and add your key. Do not commit it.
GROQ_API_KEY=your_groq_api_key_here

# Optional: use a Groq-supported chat model available to your account.
GROQ_MODEL=openai/gpt-oss-20b
