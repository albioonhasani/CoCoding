import type { VercelRequest, VercelResponse } from '@vercel/node'
import type { TutorResult } from './types'
const maxBodyBytes = 16_384
type Endpoint = (body: unknown, client: string) => Promise<TutorResult>
export async function apiHandler(request: VercelRequest, response: VercelResponse, endpoint: Endpoint) { if (request.method !== 'POST') return response.status(405).setHeader('Allow', 'POST').end(); if (!request.headers['content-type']?.includes('application/json')) return response.status(415).json({ error: 'Content-Type must be application/json.' }); const contentLength = Number(request.headers['content-length'] ?? 0); const bodySize = Buffer.byteLength(typeof request.body === 'string' ? request.body : JSON.stringify(request.body ?? '')); if (contentLength > maxBodyBytes || bodySize > maxBodyBytes) return response.status(413).json({ error: 'Request body is too large.' }); try { const result = await endpoint(request.body, request.headers['x-forwarded-for']?.toString().split(',')[0] ?? 'anonymous'); return response.status(result.status).setHeader('Cache-Control', 'no-store').setHeader('X-Content-Type-Options', 'nosniff').json(result.body) } catch { return response.status(500).setHeader('Cache-Control', 'no-store').setHeader('X-Content-Type-Options', 'nosniff').json({ error: 'The AI service is temporarily unavailable.' }) } }
