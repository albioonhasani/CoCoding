import { useId, useState } from 'react'
import type { ReactNode } from 'react'
export type Tab = { id: string; label: string; content: ReactNode }
export function Tabs({ tabs }: { tabs: Tab[] }) { const [active, setActive] = useState(tabs[0]?.id ?? ''); const uid = useId(); const selected = tabs.find((tab) => tab.id === active); return <div><div role="tablist" aria-label="Content sections" className="flex gap-1 overflow-x-auto border-b border-slate-200 dark:border-slate-800">{tabs.map((tab) => <button key={tab.id} type="button" role="tab" id={`${uid}-${tab.id}`} aria-selected={active === tab.id} aria-controls={`${uid}-${tab.id}-panel`} onClick={() => setActive(tab.id)} className={`shrink-0 border-b-2 px-4 py-3 text-sm font-semibold ${active === tab.id ? 'border-brand-600 text-brand-700 dark:text-blue-300' : 'border-transparent text-slate-600 hover:text-ink dark:text-slate-300 dark:hover:text-white'}`}>{tab.label}</button>)}</div>{selected && <div role="tabpanel" id={`${uid}-${selected.id}-panel`} aria-labelledby={`${uid}-${selected.id}`} className="pt-5">{selected.content}</div>}</div> }
