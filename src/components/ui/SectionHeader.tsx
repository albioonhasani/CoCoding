import type { ReactNode } from 'react'
export function SectionHeader({ eyebrow, title, description, action, headingLevel = 'h2' }: { eyebrow?: string; title: string; description?: string; action?: ReactNode; headingLevel?: 'h1' | 'h2' }) { const Heading = headingLevel; return <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{eyebrow && <p className="text-sm font-bold tracking-wide text-brand-700 dark:text-blue-300">{eyebrow.toUpperCase()}</p>}<Heading className="mt-1 text-2xl font-bold tracking-tight text-ink dark:text-white">{title}</Heading>{description && <p className="mt-2 max-w-2xl leading-7 text-slate-600 dark:text-slate-300">{description}</p>}</div>{action}</div> }
